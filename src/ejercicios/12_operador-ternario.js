const activo = true;
/* 
let mensaje = "";

if (activo) {
  mensaje = "Usuario Activo";
} else {
  mensaje = "Usuario no Logueado";
}
 */

// const mensaje = activo ? "Usuario Activo" : "Usuario No Logueado";
const mensaje = activo && "Usuario Activo";

console.log(mensaje);
